<%= form_for @image, html: {multipart: true} do |image| %>
  <div class="large-4 columns">
    <%= image.label :name %>
    <%= image.text_field :name %>
    <%= image.label :image %>
    <%= image.file_field :image %>
    <%= image.submit :class => 'button small round' %>
  </div>
<% end %>