<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />

  <title>InstagramChallenge2</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag "vendor/modernizr" %>
  <%= csrf_meta_tags %>
</head>

<body>

  <div class="sticky">
    <nav class="top-bar" data-topbar role="navigation">
      <ul class="title-area">
        <li class="name">
          <h1><a href="/images">Instagram</a></h1>
        </li>
        <li class="toggle-topbar menu-icon"><a href="#"><span>Menu</span></a></li>
      </ul>

      <section class="top-bar-section">
        <ul class="right">
          <% if user_signed_in? %>
          <li class="active"><a href="#"><%= link_to "Sign out", destroy_user_session_path, method: :delete %></a></li>
          <% else %>
          <li class="active"><a href="#"><%= link_to "Sign in", new_user_session_path %></a></li>
          <li class="active"><a href="#"><%= link_to "Sign up", new_user_registration_path %></a></li>
          <% end %>
        </ul>

        <ul class="left">
          <% if user_signed_in? %>
          <li class="active"><a href="/images/new">Add an image</a></li>
          <% end %>
        </ul>
      </section>
    </nav>
  </div>

  <%= notice %>
  <%= alert %>
  <%= yield %>

</body>
</html>
